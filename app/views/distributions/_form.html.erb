<%= form_for @distribution, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :value, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :value, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :interval, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :interval, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :wideness, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :wideness, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                distributions_path, :class => 'btn' %>
  </div>
<% end %>
